<selector xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- un selected -->
	<item android:state_selected="false" android:color="@color/text_light_dark" />
	<!-- selected -->
	<item android:state_selected="true" android:color="@color/common_blue" />
	<!-- default 这个要在最底下，不然没有效果-->
	<item android:color="@color/text_light_dark" />
</selector>
